@model IEnumerable<ProjetoLeilaoWeb.Models.Lance>


@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Pessoa.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model._Produto.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PessoaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProdutoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LanceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LanceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LanceID })
        </td>
    </tr>
}

</table>
