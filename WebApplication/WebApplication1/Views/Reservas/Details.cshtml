@model BaseModels.Reserva

@{
    ViewBag.Title = "Detalhes";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>Reserva</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model._Cliente.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model._Cliente.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model._Servico.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model._Servico.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Data)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Data)
        </dd>

    </dl>
</div>
<p>
    <button type="button" class="btn btn-default" aria-label="Left Align">
        <span onclick="location.href='@Url.Action("Edit","Reservas", new { id = Model.ReservaID })'" 
              class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    </button>
    <button type="button" class="btn btn-default" aria-label="Left Align">
        <span onclick="location.href='@Url.Action("Index","Reservas")'"
              class="glyphicon glyphicon-th-list" aria-hidden="true"></span>
    </button>

    @*@Html.ActionLink("Editar", "Edit", new { id = Model.ReservaID }) |
      @Html.ActionLink("Retornar para Lista", "Index")*@
</p>
